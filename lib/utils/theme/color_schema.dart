import 'package:flutter/material.dart';

const lightColorSchema = ColorScheme.light(primary: Colors.black, surface: Colors.white);
const darkColorSchema = ColorScheme.light(primary: Colors.white, surface: Colors.black);
